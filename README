NetServer::ProcessTop

This module sets up a mini-server accessible via telnet that shows
what a process's event loop is doing.  The output is formatted similar
to the popular 'top' program.  Event priorities and other parameters
can be changed interactively.


Requires Event 0.11.
